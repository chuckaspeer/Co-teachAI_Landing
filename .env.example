# MongoDB (required for leads API)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_OPS=coteachai_ops

# Optional: POST lead payload to this URL after saving to MongoDB (e.g. Google Apps Script web app). Failures are logged only.
# GOOGLE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/.../exec
